CREATE TABLE bookshelf (
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    author TEXT NOT NULL,
    url TEXT NOT NULL,
    image TEXT NOT NULL
);

select_row:
SELECT * FROM bookshelf WHERE _id = ?;

insert_row:
INSERT INTO bookshelf (title, author, url, image) VALUES (?, ?, ?, ?);