CREATE TABLE bookshelf_header_image (
    url TEXT NOT NULL,
    image TEXT NOT NULL
);

select_all:
SELECT * FROM bookshelf_header_image;

insert_row:
INSERT INTO bookshelf_header_image (url, image) VALUES (?, ?);