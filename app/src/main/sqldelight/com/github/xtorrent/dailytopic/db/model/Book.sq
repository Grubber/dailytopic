CREATE TABLE book (
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    author TEXT NOT NULL,
    url TEXT NOT NULL,
    image TEXT NOT NULL
);

select_row:
SELECT * FROM book WHERE url = ?;

insert_row:
INSERT INTO book (title, author, url, image) VALUES (?, ?, ?, ?);

count_row:
SELECT COUNT (*) FROM book WHERE url = ?;